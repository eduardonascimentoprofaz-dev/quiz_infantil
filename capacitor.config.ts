import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.quizinfantil.app',
  appName: 'Quiz Infantil',
  webDir: 'dist/public'
};

export default config;
